# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# Modified for a static HTML/JS Bug Catcher game (no build step)

name: Deploy to Firebase Hosting on PR

on:
  pull_request:

permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  build_and_preview:
    # Only run if the PR comes from this repository (not from a fork)
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # No real build needed – static HTML/JS game
      - name: Build static site (no-op)
        run: echo "No build step needed for Bug Catcher game."

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BUG_CATCHER_GAME }}
          projectId: bug-catcher-game
          target: bugCatcher
          # Create a unique preview channel per PR
          channelId: pr-${{ github.event.number }}
